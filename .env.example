# Get these from your Supabase project settings (API section).
# SUPABASE_SERVICE_ROLE_KEY must stay server-side only — never prefix it
# with NEXT_PUBLIC_, or it ships to the browser and bypasses RLS for anyone.
SUPABASE_URL=https://avvxkqvuzrsdeerlyszx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
